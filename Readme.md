# Bean Hot Deployer

Bean deployer without restarting the tomcat server.  


# Usage

This is a Jar library that can be imported in any maven project as follows:

```
<dependency>
    <groupId>com.biit-solutions</groupId>
    <artifactId>user-manager</artifactId>
</dependency>
```